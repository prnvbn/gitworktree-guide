{
  "name": "git-worktree-simple-guide",
  "version": "1.0.0",
  "private": true,
  "description": "A short, visual guide to Git worktrees.",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17"
  }
}
